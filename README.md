# 🚗 CRUD de CNH com Flask (Padrão MVC)

Um projeto simples em **Python Flask** que implementa um **CRUD completo** de CNHs (Carteiras Nacionais de Habilitação) seguindo o padrão **MVC (Model-View-Controller)**.

---

## 📁 Estrutura do Projeto

flask-crud-cnh/
│
├── app.py # Arquivo principal (Controller)
├── model/
│ └── cnh_model.py # Camada de Model (Banco de Dados)
└── service/
└── cnh_service.py # Camada de Service (Regras e Operações)

## 🚀 Tecnologias Utilizadas

- Python 3.10+
- Flask
- SQLite